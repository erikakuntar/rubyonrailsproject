<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <p class="comment"><%= comment.body %></p>
  <%= from_time = Time.at(@comment.article.created_at) %>

  <p>
    Posted <%= distance_of_time_in_words(from_time, from_time + 50.minutes) %> later
  </p>
</div>
